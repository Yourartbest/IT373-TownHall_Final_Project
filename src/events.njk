---
layout: layouts/base.njk
title: Upcoming Events - Newark AI Town Hall
description: Join us for AI education workshops, town halls, and community events in Newark, NJ. Free and open to all community members.
permalink: /events/
---

<section class="hero" aria-labelledby="events-hero-heading">
  <div class="container">
    <h1 id="events-hero-heading" class="hero-title">UPCOMING EVENTS</h1>
    <p class="hero-text">
      Join us for workshops, town halls, and community gatherings focused on making AI accessible to everyone in Newark.
    </p>
  </div>
</section>

<section class="events" aria-labelledby="events-heading">
  <div class="container">
    <h2 id="events-heading" class="section-heading">ALL EVENTS</h2>
    {% if events.length > 0 %}
      <div class="events-grid">
        {% for event in events %}
          <article class="event-card" aria-labelledby="event-{{ loop.index }}-title">
            {% if event.imageUrl %}
            <img
              src="{{ event.imageUrl }}?w=400&h=250&fit=crop&auto=format"
              alt="{{ event.imageAlt or event.title }}"
              class="event-card-image"
              loading="lazy"
              width="400"
              height="250"
            />
            {% endif %}

            <div class="event-card-content">
              <div class="event-card-meta">
                <time datetime="{{ event.date | dateISO }}">{{ event.date | dateFormat }}</time>
                {% if event.difficultyLevel %}
                <span class="badge badge-{{ event.difficultyLevel }}">{{ event.difficultyLevel }}</span>
                {% endif %}
              </div>

              <h3 id="event-{{ loop.index }}-title" class="event-card-title">{{ event.title }}</h3>

              <p class="event-card-description">{{ event.description }}</p>

              {% if event.location %}
              <p class="event-card-location">📍 {{ event.location }}</p>
              {% endif %}

              <a href="/events/{{ event.slug }}/" class="btn btn-outline">
                Learn More<span class="sr-only"> about {{ event.title }}</span>
              </a>
            </div>
          </article>
        {% endfor %}
      </div>
    {% else %}
      <div class="events-empty">
        <p>
          No upcoming events scheduled yet. Check back soon or
          <a href="/volunteer/">volunteer to help organize</a>!
        </p>
      </div>
    {% endif %}

    <div class="events-footer">
      <a href="https://discord.gg/newark-ai" class="btn btn-primary" target="_blank" rel="noopener noreferrer">
        Join Discord Community
      </a>
    </div>
  </div>
</section>

<section class="cta" aria-labelledby="host-cta-heading">
  <div class="container">
    <h2 id="host-cta-heading" class="cta-heading">WANT TO HOST AN EVENT?</h2>
    <p class="cta-text">
      We're always looking for community partners and speakers to help make AI education accessible.
    </p>
    <div class="cta-buttons">
      <a href="/volunteer/" class="btn btn-primary-inverse">Get Involved</a>
    </div>
  </div>
</section>
