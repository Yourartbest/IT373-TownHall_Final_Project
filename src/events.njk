---
layout: layouts/base.njk
title: Upcoming Events - Newark AI Town Hall
description: Join us for AI education workshops, town halls, and community events in Newark, NJ. Free and open to all community members.
permalink: /events/
---

<section class="mission" aria-labelledby="events-hero-heading" style="padding: var(--grid-8) 0;">
  <div class="container">
    <div class="accent-bar"></div>
    <h1 id="events-hero-heading" style="font-size: clamp(32px, 5vw, 48px); margin-bottom: var(--grid-3);">UPCOMING EVENTS</h1>
    <p style="font-size: 16px; line-height: 1.5; max-width: 600px;">
      Join us for free, friendly learning sessions about AI. All events are designed for beginners and low-tech community members.
    </p>
  </div>
</section>

<section class="events" aria-labelledby="filter-heading">
  <div class="container">
    {# Filter Bar #}
    <div style="background: var(--color-white); border: var(--border-width) solid var(--color-black); padding: var(--grid-3); margin-bottom: var(--grid-6); display: flex; align-items: center; gap: var(--grid-3); flex-wrap: wrap;">
      <div style="display: flex; align-items: center; gap: var(--grid-2);">
        <div style="width: 40px; height: 40px; background: var(--color-bauhaus-yellow); border: var(--border-width) solid var(--color-black); display: flex; align-items: center; justify-content: center; font-size: 20px;">▼</div>
        <span style="font-weight: 700; text-transform: uppercase; letter-spacing: 0.05em;">FILTER EVENTS</span>
      </div>
      <div style="display: flex; gap: var(--grid-2); flex-wrap: wrap; flex: 1;">
        <button class="btn btn-primary" style="padding: var(--grid-unit) var(--grid-2); min-height: auto;">ALL</button>
        <button class="btn btn-outline" style="padding: var(--grid-unit) var(--grid-2); min-height: auto;">BEGINNER-FRIENDLY</button>
        <button class="btn btn-outline" style="padding: var(--grid-unit) var(--grid-2); min-height: auto;">FAMILY-FRIENDLY</button>
        <button class="btn btn-outline" style="padding: var(--grid-unit) var(--grid-2); min-height: auto;">EDUCATORS</button>
        <button class="btn btn-outline" style="padding: var(--grid-unit) var(--grid-2); min-height: auto;">YOUTH</button>
        <button class="btn btn-outline" style="padding: var(--grid-unit) var(--grid-2); min-height: auto;">VOLUNTEER</button>
      </div>
    </div>

    {# Event Count #}
    <div style="display: flex; align-items: center; gap: var(--grid-2); margin-bottom: var(--grid-4);">
      <div style="width: 12px; height: 12px; background: var(--color-bauhaus-red); border-radius: 50%;"></div>
      <span style="font-weight: 700; text-transform: uppercase;">{{ events.length }} EVENTS</span>
    </div>
    {% if events.length > 0 %}
      <div class="events-grid">
        {% for event in events %}
          <article class="card" aria-labelledby="event-{{ loop.index }}-title" style="padding: var(--grid-4);">
            <h3 id="event-{{ loop.index }}-title" style="font-size: 20px; font-weight: 700; text-transform: uppercase; margin-bottom: var(--grid-3); line-height: 1.3;">{{ event.title }}</h3>

            {# Date and Location with icons #}
            <div style="margin-bottom: var(--grid-3);">
              <div style="display: flex; align-items: center; gap: var(--grid-2); margin-bottom: var(--grid-unit);">
                <div style="width: 8px; height: 8px; background: var(--color-bauhaus-red); border-radius: 50%;"></div>
                <svg width="16" height="16" fill="currentColor" aria-hidden="true" style="opacity: 0.7;">
                  <rect x="3" y="4" width="10" height="10" rx="1" fill="none" stroke="currentColor" stroke-width="1.5"/>
                  <line x1="3" y1="7" x2="13" y2="7" stroke="currentColor" stroke-width="1.5"/>
                </svg>
                <time datetime="{{ event.date | dateISO }}" style="font-size: 14px;">{{ event.date | dateFormat }} • 2:00 PM</time>
              </div>
              <div style="display: flex; align-items: center; gap: var(--grid-2);">
                <div style="width: 8px; height: 8px; background: var(--color-bauhaus-red); border-radius: 50%;"></div>
                <svg width="16" height="16" fill="currentColor" aria-hidden="true" style="opacity: 0.7;">
                  <path d="M8 2C6 2 4 3.5 4 6c0 3 4 8 4 8s4-5 4-8c0-2.5-2-4-4-4zm0 6c-1.1 0-2-.9-2-2s.9-2 2-2 2 .9 2 2-.9 2-2 2z"/>
                </svg>
                <span style="font-size: 14px;">{{ event.location or "Newark Public Library" }}</span>
              </div>
            </div>

            {# Category Badge #}
            {% if event.difficultyLevel %}
            <div style="margin-bottom: var(--grid-3);">
              <span class="badge badge-{{ event.difficultyLevel }}" style="background: var(--color-bauhaus-red); color: white; padding: var(--grid-unit) var(--grid-2); text-transform: uppercase; font-size: 12px; font-weight: 700; letter-spacing: 0.05em;">{{ event.difficultyLevel }}-FRIENDLY</span>
            </div>
            {% endif %}

            {# Description #}
            <p style="font-size: 15px; line-height: 1.6; margin-bottom: var(--grid-3); color: #333;">{{ event.description or "Learn what AI really is and how it affects your daily life. No technical background required." }}</p>

            {# Tags #}
            <div style="display: flex; gap: var(--grid-2); flex-wrap: wrap; margin-bottom: var(--grid-4);">
              {% if event.difficultyLevel %}
              <button class="btn btn-outline" style="padding: var(--grid-unit) var(--grid-2); min-height: auto; font-size: 12px;">{{ event.difficultyLevel | upper }}-FRIENDLY</button>
              {% endif %}
              {% if event.audienceTag %}
              <button class="btn btn-outline" style="padding: var(--grid-unit) var(--grid-2); min-height: auto; font-size: 12px;">{{ event.audienceTag | upper }}</button>
              {% endif %}
            </div>

            {# CTAs #}
            <div style="display: flex; gap: var(--grid-2); align-items: center; padding-top: var(--grid-3); border-top: 2px solid #eee;">
              <a href="/events/{{ event.slug }}/" style="text-decoration: none; color: var(--color-bauhaus-blue); font-weight: 700; font-size: 14px; text-transform: uppercase; letter-spacing: 0.05em;">DETAILS →</a>
              <a href="/events/{{ event.slug }}/" class="btn btn-primary" style="margin-left: auto; padding: var(--grid-2) var(--grid-4);">REGISTER</a>
            </div>
          </article>
        {% endfor %}
      </div>
    {% else %}
      <div class="events-empty">
        <p>
          No upcoming events scheduled yet. Check back soon or
          <a href="/volunteer/">volunteer to help organize</a>!
        </p>
      </div>
    {% endif %}
  </div>
</section>
